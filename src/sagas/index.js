import { all, fork } from 'redux-saga/effects';


export default function* root() {
  // yield all([
  //   fork(home),
  //   fork(earth),
  //   fork(places),
  //   fork(indexes),
  //   fork(experience)
  // ])
}
